# 真实模型实验 #14：模型忽略 Schema 长度约束

## 固定条件

- 时间：2026-07-18 08:17:20（Asia/Shanghai）
- 模型：`glm-5.2`（通过 Anthropic Messages 协议网关调用）
- Provider：`anthropic-messages`，路径 `/v1/messages`
- Prompt 版本：`anthropic-tool-v3`（增加 `sourceCode.maxLength=10000`）
- 目标：尽量扩大可活动区域，避免进入狭窄通道；在多个安全方向中优先保留后续选择。
- 最大迭代：3；公开集 48 局／版本；隐藏集 24 局／合格版本

## 结果

- 任务：#14
- 状态：`FAILED`
- 当前轮次：2/3
- 总耗时：74 秒
- 错误码：`EVALUATION_TIMEOUT`（当时的异常映射；实际原因为源码长度校验失败）
- 错误摘要：`源码超过 10000 字符`

| 版本 | 公开得分 | 隐藏得分 | 胜率 | 合法移动率 | P95 | Token | 是否接受 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| V1 | 0.0104 | 未验证 | 0.0000 | 100% | 1ms | 1850 | 否 |
| V2 | 0.0104 | 未验证 | 0.0000 | 100% | 1ms | 4109 | 否 |

## 诊断与处置

结果再次证明当前模型／网关不会强制执行工具 JSON Schema 的 `maxLength`。服务端仍拒绝超长源码，没有截断或放宽边界。后续增加一次有限的决策校验反馈重试，并把耗尽错误规范为 `LLM_INVALID_RESPONSE`。
